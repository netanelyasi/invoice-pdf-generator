<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Template - <%= name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .editor { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; }
        textarea { min-height: 60vh; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">📄 מחולל חשבוניות</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/docs">API Docs</a>
                <a class="nav-link" href="/templates">Templates</a>
            </div>
        </div>
    </nav>

    <div class="container py-4">
        <div class="d-flex align-items-center justify-content-between mb-3">
            <h1 class="h3 mb-0">Edit Template: <code><%= name %></code></h1>
            <a href="/templates" class="btn btn-outline-secondary">חזרה</a>
        </div>

        <div class="alert alert-info">
            השינויים נשמרים ישירות לקובץ <code>src/templates/<%= name %>.hbs</code>. מומלץ שימוש בבקרת תצורה לפני עריכה בייצור.
        </div>

        <form id="editorForm" class="editor">
            <div class="mb-3">
                <label class="form-label">תוכן התבנית</label>
                <textarea id="templateContent" class="form-control" spellcheck="false"><%- content %></textarea>
            </div>
            <div class="d-flex gap-2">
                <button type="button" class="btn btn-primary" onclick="handleSave()">שמירה</button>
                <button type="button" class="btn btn-secondary" onclick="handlePreview()">תצוגה מקדימה</button>
                <a href="/templates" class="btn btn-outline-secondary">ביטול</a>
            </div>
        </form>
    </div>

    <script>
        const handleSave = () => {
            const content = document.getElementById('templateContent').value;
            fetch(`/api/templates/<%= name %>`, {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ content })
            })
            .then(res => res.json())
            .then(data => {
                if (!data.success) throw new Error(data.error || 'Save failed');
                alert('Template saved successfully');
            })
            .catch(err => alert(err.message));
        };

        const handlePreview = () => {
            const sample = {
                templateType: '<%= name %>',
                customer: { name: 'Preview Customer' },
                items: [{ description: 'Item', quantity: 1, rate: 1, amount: 1 }],
                total: 1
            };
            fetch('/api/preview', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(sample)
            })
            .then(r => r.text())
            .then(html => {
                const w = window.open('', '_blank');
                w.document.write(html);
                w.document.close();
            });
        };
    </script>
    <footer class="bg-light text-center text-muted py-3 mt-4">
      דף זה נבנה ע''י BrainboxAI
    </footer>
</body>
</html>


